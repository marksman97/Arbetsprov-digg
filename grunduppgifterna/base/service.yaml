apiVersion: v1
kind: Service
metadata:
  name: arbetsprov-app
  labels:
    app: arbetsprov-app
spec:
  selector:
    app: arbetsprov-app
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
